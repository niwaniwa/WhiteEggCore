# plugin lock
lock: false
# settings...
setting:
  # database setting
  database:
    # MySQL, MongoDB.
    type: ''
  # player setting
  player:
    # use database? check database setting
    savePlayerData: true
    # default `plugins/WhiteEggCore/players/`
    path: ''
    autoSave:
      enable: false
      # custom time
      time: 3600 # second
  script:
    enableScript: false
    # default `script/`
    path: ''
  # do not edit this
  enablecCommands: true
  twitter:
    # enable?
    useTwitter: true
    # twitter app
    consumerKey: ''
    consumerSecret: ''
